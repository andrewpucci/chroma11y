version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: tuesday
      time: '08:00'
      timezone: America/Los_Angeles
    open-pull-requests-limit: 2
    labels:
      - dependencies
      - ci
    assignees:
      - andrewpucci
    commit-message:
      prefix: ci
      include: scope
    groups:
      github-actions:
        patterns:
          - '*'
      github-actions-security:
        applies-to: security-updates
        patterns:
          - '*'

  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: tuesday
      time: '09:00'
      timezone: America/Los_Angeles
    open-pull-requests-limit: 3
    labels:
      - dependencies
    assignees:
      - andrewpucci
    # Keep manifest ranges stable when they already admit the new release;
    # Dependabot still updates the lockfile/resolved version.
    versioning-strategy: increase-if-necessary
    # SvelteKit and svelte-check do not yet publish TypeScript 7 support.
    # Keep 6.x moving, but block new 7.x PRs until the watcher workflow opens
    # a follow-up issue telling us to remove this ignore.
    ignore:
      - dependency-name: typescript
        update-types:
          - version-update:semver-major
      - dependency-name: '@types/node'
        update-types:
          - version-update:semver-major
    commit-message:
      prefix: chore
      prefix-development: chore
      include: scope
    groups:
      npm-production-minor-patch:
        dependency-type: production
        patterns:
          - '*'
        update-types:
          - minor
          - patch
      npm-development-minor-patch:
        dependency-type: development
        patterns:
          - '*'
        update-types:
          - minor
          - patch
      npm-security-production:
        applies-to: security-updates
        dependency-type: production
        patterns:
          - '*'
      npm-security-development:
        applies-to: security-updates
        dependency-type: development
        patterns:
          - '*'
